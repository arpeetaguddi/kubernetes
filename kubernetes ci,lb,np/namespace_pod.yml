apiVersion: v1
kind: Service
metadata:
  name: clusteripsvc
  namespace: dev   # 👈 namespace added here
spec:
  type: ClusterIP
  selector:
    app: myapp
  ports:
    - port: 80
      targetPort: 8080
      